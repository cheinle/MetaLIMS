2015/04/13
Folder contains:
-Collocation folder with raw data from collocation and the actual collocation factors
-raw_OPC_data folder with the raw data downloaded from the OPC during the experiement (no data has been updated with collocation factors)
-raw_OPC_data_rain_status_appened folder with the same data as in the raw_OPC_data folder but with the rain amount per hour in mm appended in a column appended to the end of the row.

-CH